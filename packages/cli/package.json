{
  "name": "@bookstory/cli",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "bookstory": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --watch",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@bookstory/core": "workspace:*",
    "commander": "^13",
    "glob": "^11",
    "chalk": "^5"
  },
  "devDependencies": {
    "tsup": "^8",
    "typescript": "^5",
    "@types/node": "^22"
  }
}
